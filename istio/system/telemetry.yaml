apiVersion: telemetry.istio.io/v1
kind: Telemetry
metadata:
  name: custom-metrics
  namespace: istio-system
spec:
  metrics:
    - providers:
        - name: prometheus
      overrides:
        - match:
            mode: CLIENT
            metric: ALL_METRICS
          tagOverrides:
            source_app:
              value: "has(source.labels[\"app.kubernetes.io/name\"]) ? source.labels[\"app.kubernetes.io/name\"].value : 'test'"
        - match:
            mode: SERVER
            metric: ALL_METRICS
          tagOverrides:
            source_app:
              operation: UPSERT
              value: "has(source.labels[\"app.kubernetes.io/name\"]) ? source.labels[\"app.kubernetes.io/name\"].value : 'test'"
